name: Fab amoeba on push KiCad
on:
  push:
    paths:
    - 'amoeba_choc14/*.kicad_sch'
    - 'amoeba_choc14/*.kicad_pcb'
  workflow_dispatch:

jobs:
  call-fab:
    name: Call Fabricate
    uses: ./.github/workflows/fabricate.yaml
    with:
      board: amoeba_choc14
